name: CI

on: [push, pull_request, workflow_dispatch]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: 'npm' }
      - run: npm ci
      - run: npm audit --audit-level=high --omit=dev
        continue-on-error: true
      - run: npm run lint
      - run: npm run check:boundaries
      - run: python3 scripts/verify_tenant_isolation.py src
      - run: npm run typecheck
      - run: npm run test --workspace=@eshule/web-react
      - run: npm run build

  e2e:
    runs-on: ubuntu-latest
    # Playwright E2E needs a live Supabase project + seeded test user. Gate on the
    # secret being present so forks / secretless PRs skip instead of failing red.
    if: ${{ github.event_name == 'push' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: 'npm' }
      - run: npm ci
      - name: Check E2E secrets
        id: guard
        env:
          SUPABASE_URL: ${{ secrets.PUBLIC_SUPABASE_URL }}
        run: |
          if [ -z "$SUPABASE_URL" ]; then
            echo "E2E secrets not configured — skipping."
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
      - name: Install Playwright browsers
        if: steps.guard.outputs.run == 'true'
        run: npm run test:e2e:install
      - name: Build
        if: steps.guard.outputs.run == 'true'
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL || secrets.PUBLIC_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY || secrets.PUBLIC_SUPABASE_ANON_KEY }}
        run: npm run build
      - name: Run Playwright E2E
        if: steps.guard.outputs.run == 'true'
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL || secrets.PUBLIC_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY || secrets.PUBLIC_SUPABASE_ANON_KEY }}
          TEST_USER_EMAIL: ${{ secrets.TEST_USER_EMAIL }}
          TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
        run: npm run test:e2e --workspace=@eshule/web-react || npx playwright test --config apps/web-react/playwright.config.ts
      - name: Upload Playwright report
        if: ${{ always() && steps.guard.outputs.run == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  database:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Start local Supabase
        run: supabase start
      - name: Replay migrations and seed
        run: supabase db reset --local --yes
      - name: Run cross-tenant isolation checks
        run: supabase db query --local --file supabase/tests/tenant_isolation.sql
