# Architecture documentation moved

The authoritative architecture reference is [`../../ARCHITECTURE.md`](../../ARCHITECTURE.md).

This legacy duplicate is intentionally reduced to a pointer so obsolete architecture claims are not mistaken for the current implementation.
