import { z } from 'zod/v3';
import { fail } from '@sveltejs/kit'; import type { PageServerLoad, Actions } from './$types'; import { requireTenantRole } from '$lib/server/_auth/auth'; import { parseForm } from '$lib/server/_platform/validation'; import { getStudentsByTenant } from '$lib/server/_sis/students'; import { PAGE_LIST_MEDIUM } from '$lib/config';
const studentSchema=z.object({id:z.string().optional(),first_name:z.string().min(1,'First name is required').max(100),middle_name:z.string().max(100).optional(),last_name:z.string().min(1,'Last name is required').max(100),admission_no:z.string().min(1,'Admission no is required').max(50),student_no:z.string().max(50).optional(),date_of_birth:z.string().optional(),gender:z.string().max(30).optional(),admission_date:z.string().optional(),nationality:z.string().max(80).optional(),birth_certificate_no:z.string().max(80).optional(),grade:z.string().max(50).optional(),status:z.enum(['active','inactive'])});
const deleteSchema=z.object({id:z.string()});
export const load:PageServerLoad=async({locals,url})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const page=Math.max(1,parseInt(url.searchParams.get('page')??'1'));const search=url.searchParams.get('search')??'';const sortKey=url.searchParams.get('sort');const sortDir=url.searchParams.get('dir')==='desc'?'desc':'asc';const result=await getStudentsByTenant(locals.srv,tenantId,{page,pageSize:PAGE_LIST_MEDIUM,search,sortKey,sortDir});return{students:result.data,pagination:{total:result.total,page:result.page,pageSize:result.pageSize,search,sortKey,sortDir:sortDir as 'asc'|'desc'}};};
export const actions={create:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const v=parseForm(studentSchema,await request.formData());if(!v.success)return fail(400,{errors:v.errors});const{error}=await locals.srv.from('students').insert({tenant_id:tenantId,first_name:v.data.first_name,middle_name:v.data.middle_name||null,last_name:v.data.last_name,admission_no:v.data.admission_no,student_no:v.data.student_no||null,date_of_birth:v.data.date_of_birth||null,gender:v.data.gender||null,admission_date:v.data.admission_date||null,nationality:v.data.nationality||null,birth_certificate_no:v.data.birth_certificate_no||null,grade:v.data.grade||null,status:v.data.status??'active'});if(error)return fail(error.code==='23505'?409:500,{message:error.code==='23505'?'A student with this admission or student number already exists.':`Failed: ${error.message}`});return{success:true,message:'Student created successfully'};},update:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const v=parseForm(studentSchema,await request.formData());if(!v.success)return fail(400,{errors:v.errors});if(!v.data.id)return fail(400,{message:'ID required'});const{error}=await locals.srv.from('students').update({first_name:v.data.first_name,middle_name:v.data.middle_name||null,last_name:v.data.last_name,admission_no:v.data.admission_no,student_no:v.data.student_no||null,date_of_birth:v.data.date_of_birth||null,gender:v.data.gender||null,admission_date:v.data.admission_date||null,nationality:v.data.nationality||null,birth_certificate_no:v.data.birth_certificate_no||null,grade:v.data.grade||null,status:v.data.status??'active'}).eq('id',v.data.id).eq('tenant_id',tenantId);if(error)return fail(error.code==='23505'?409:500,{message:`Failed: ${error.message}`});return{success:true,message:'Student updated successfully'};},delete:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const v=parseForm(deleteSchema,await request.formData());if(!v.success)return fail(400,{errors:v.errors});const{error}=await locals.srv.from('students').update({deleted_at:new Date().toISOString(),status:'inactive',archived_at:new Date().toISOString()}).eq('id',v.data.id).eq('tenant_id',tenantId).is('deleted_at',null);if(error)return fail(500,{message:`Failed: ${error.message}`});return{success:true,message:'Student archived successfully; history is retained.'};}} satisfies Actions;
