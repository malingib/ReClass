import { z } from 'zod/v3';
import { fail } from '@sveltejs/kit'; import type { PageServerLoad, Actions } from './$types';
import { requireTenantRole } from '$lib/server/_auth/auth'; import { parseForm } from '$lib/server/_platform/validation'; import { createEnrollment, getEnrollmentOptions, getStudentsForEnrollment } from '$lib/server/_sis/lifecycle';
const schema=z.object({student_id:z.string().min(1),class_id:z.string().min(1),academic_year:z.string().min(1).max(30),enrolled_on:z.string().min(1)});
export const load:PageServerLoad=async({locals})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const[classes,students]=await Promise.all([getEnrollmentOptions(locals.srv,tenantId),getStudentsForEnrollment(locals.srv,tenantId)]);const db=locals.srv as unknown as{from:(table:string)=>any};const{data:enrollments}=await db.from('sis_enrollments').select('id,student_id,class_id,academic_year,status,enrolled_at,exited_at,students(first_name,last_name,admission_no),sis_classes(name,stream,code)').eq('tenant_id',tenantId).order('enrolled_at',{ascending:false}).limit(300);return{classes,students,enrollments:enrollments??[]};};
export const actions={create:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const v=parseForm(schema,await request.formData());if(!v.success)return fail(400,{errors:v.errors});const result=await createEnrollment(locals.srv,{tenantId,studentId:v.data.student_id,classId:v.data.class_id,academicYear:v.data.academic_year,enrolledOn:v.data.enrolled_on,actorId:locals.user?.id??null});if(!result.ok)return fail(result.error.code==='23505'?409:400,{message:result.error.message??'Could not enroll student.'});return{success:true,message:'Student enrolled. Previous enrollment history remains unchanged.'};}} satisfies Actions;
