import type { PageServerLoad } from './$types'; import { requireTenantRole } from '$lib/server/_auth/auth';
export const load:PageServerLoad=async({locals,url})=>{const {tenantId}=requireTenantRole(locals,'school_admin','super_admin','principal','bursar');const module=url.searchParams.get('module');let q=locals.srv.from('audit_logs').select('id,actor_user_id,actor_label,action,module,entity_type,entity_id,target_label,reason,before_data,after_data,source,result,created_at').eq('tenant_id',tenantId).order('created_at',{ascending:false}).limit(500);if(module)q=q.eq('module',module);const {data}=await q;return{logs:data??[],module};};
