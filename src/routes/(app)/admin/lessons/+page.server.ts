import { z } from 'zod/v3'; import { fail } from '@sveltejs/kit'; import type { PageServerLoad, Actions } from './$types'; import { requireTenantRole } from '$lib/server/_auth/auth'; import { parseForm } from '$lib/server/_platform/validation';
const schema=z.object({teacher_id:z.string().min(1),subject_id:z.string().optional(),year_group_id:z.string().optional(),stream_id:z.string().optional(),starts_at:z.string().min(1),ends_at:z.string().min(1),room:z.string().max(100).optional(),notes:z.string().max(1000).optional()});
export const load:PageServerLoad=async({locals})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const db=locals.srv as unknown as{from:(t:string)=>any};const[{data:lessons},{data:teachers},{data:subjects},{data:groups},{data:streams}]=await Promise.all([db.from('lessons').select('id,starts_at,ends_at,room,status,notes,teachers(first_name,last_name),subjects(name),year_groups(name),streams(name)').eq('tenant_id',tenantId).is('deleted_at',null).order('starts_at',{ascending:true}).limit(300),db.from('teachers').select('id,first_name,last_name').eq('tenant_id',tenantId).is('deleted_at',null).order('first_name'),db.from('subjects').select('id,name').eq('tenant_id',tenantId).is('deleted_at',null).order('name'),db.from('year_groups').select('id,name').eq('tenant_id',tenantId).is('deleted_at',null).eq('active',true).order('sort_order'),db.from('streams').select('id,name,year_group_id').eq('tenant_id',tenantId).is('deleted_at',null).eq('active',true).order('name')]);return{lessons:lessons??[],teachers:teachers??[],subjects:subjects??[],groups:groups??[],streams:streams??[]};};
export const actions={create:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const v=parseForm(schema,await request.formData());if(!v.success)return fail(400,{errors:v.errors});const db=locals.srv as unknown as{from:(t:string)=>any};const{error}=await db.from('lessons').insert({tenant_id:tenantId,teacher_id:v.data.teacher_id,subject_id:v.data.subject_id||null,year_group_id:v.data.year_group_id||null,stream_id:v.data.stream_id||null,starts_at:v.data.starts_at,ends_at:v.data.ends_at,room:v.data.room||null,notes:v.data.notes||null,created_by:locals.user?.id||null});if(error)return fail(500,{message:error.message});return{success:true,message:'Lesson scheduled.'};},cancel:async({locals,request})=>{const{tenantId}=requireTenantRole(locals,'school_admin','super_admin');const id=String((await request.formData()).get('id')??'');if(!id)return fail(400,{message:'Lesson ID required.'});const db=locals.srv as unknown as{from:(t:string)=>any};const{error}=await db.from('lessons').update({status:'cancelled',updated_at:new Date().toISOString()}).eq('id',id).eq('tenant_id',tenantId);if(error)return fail(500,{message:error.message});return{success:true,message:'Lesson cancelled.'};}} satisfies Actions;
