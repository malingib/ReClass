<script lang="ts">import DashboardContent from '$lib/components/DashboardContent.svelte';const{data}= $props();const lessons=$derived(data.lessons);const reminders=$derived(data.reminders);</script>
<DashboardContent title="My Lessons" subtitle="Upcoming school lessons and operational reminders"><div class="grid gap-4 lg:grid-cols-2"><section class="rounded-xl border border-border bg-white p-5"><h2 class="font-semibold">Upcoming Lessons</h2><div class="mt-4 space-y-3">{#each lessons as l}<div class="rounded-lg bg-muted/40 p-3"><div class="flex items-center justify-between"><p class="text-sm font-medium">{l.subjects?.name??'Lesson'} · {l.sis_classes?.name??'Class'} {l.sis_classes?.stream??''}</p><span class="text-xs capitalize">{l.status}</span></div><p class="mt-1 text-xs text-muted-foreground">{new Date(l.starts_at).toLocaleString()} → {new Date(l.ends_at).toLocaleTimeString()} · {l.room??'No room'}</p></div>{:else}<p class="text-sm text-muted-foreground">No upcoming lessons.</p>{/each}</div></section><section class="rounded-xl border border-border bg-white p-5"><h2 class="font-semibold">Reminders</h2><div class="mt-4 space-y-3">{#each reminders as r}<div class="rounded-lg bg-muted/40 p-3"><p class="text-sm font-medium">{r.title}</p><p class="mt-1 text-xs text-muted-foreground">{r.reminder_type} · {new Date(r.remind_at).toLocaleString()}</p>{#if r.body}<p class="mt-1 text-xs text-muted-foreground">{r.body}</p>{/if}</div>{:else}<p class="text-sm text-muted-foreground">No upcoming reminders.</p>{/each}</div></section></div></DashboardContent>
