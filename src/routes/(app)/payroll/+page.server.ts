import type { PageServerLoad } from './$types';
import { requireTenantRole } from '$lib/server/_auth/auth';
export const load:PageServerLoad=async({locals})=>{const {tenantId}=requireTenantRole(locals,'school_admin','super_admin','principal','bursar');const today=new Date();const day=today.getDay()||7;const start=new Date(today);start.setDate(today.getDate()-day+1);const end=new Date(start);end.setDate(start.getDate()+6);const s=start.toISOString().slice(0,10),e=end.toISOString().slice(0,10);const {data:period}=await locals.srv.from('payroll_periods').select('id,period_start,period_end,status').eq('tenant_id',tenantId).eq('period_start',s).eq('period_end',e).maybeSingle();if(!period)return{payroll:[],periodLabel:`${s} – ${e}`,status:'draft'};const {data:lines}=await locals.srv.from('payroll_lines').select('id,teacher_user_id,gross_amount,deductions,net_amount,notes').eq('payroll_id',period.id);const ids=(lines??[]).map((x:any)=>x.teacher_user_id);const {data:profiles}=ids.length?await locals.srv.from('profiles').select('id,full_name').in('id',ids):{data:[]};const names=new Map((profiles??[]).map((p:any)=>[p.id,p.full_name]));const {data:payments}=await locals.srv.from('payroll_payments').select('teacher_user_id,status,payment_reference').eq('payroll_id',period.id);const pm=new Map((payments??[]).map((p:any)=>[p.teacher_user_id,p]));const {data:receipts}=ids.length?await locals.srv.from('payment_receipts').select('teacher_user_id,receipt_number').eq('tenant_id',tenantId).eq('payment_domain','payroll').in('teacher_user_id',ids):{data:[]};const rm=new Map((receipts??[]).map((r:any)=>[r.teacher_user_id,r.receipt_number]));return{periodLabel:`${period.period_start} – ${period.period_end}`,status:period.status,payroll:(lines??[]).map((l:any)=>({...l,teacher_name:names.get(l.teacher_user_id)??'Teacher',status:pm.get(l.teacher_user_id)?.status??'not initiated',receipt_number:rm.get(l.teacher_user_id)??null}))};};
