export {
  suiteModules,
  moduleIcons,
  MODULES,
  MODULE_LIST,
  KERNEL_MODULES,
  ROLE_SURFACE,
  routeFor,
  gatedModuleForPath,
  isAlwaysOn,
  isKernel,
  canImport,
} from '@eshule/shared';

export type {
  SuiteModule,
  ModuleStatus,
  ModuleIconId,
  ModuleId,
  ModuleKind,
  ModuleAccent,
  ModuleDef,
  ServerModuleKey,
} from '@eshule/shared';
