import { paginatedQuery } from '../_platform/query';
const STUDENT_SORT_COLUMNS = new Set(['admission_no','student_no','first_name','last_name','grade','status','created_at']);
const STUDENT_SEARCH_COLUMNS = ['admission_no','student_no','first_name','middle_name','last_name','grade'];
export async function getStudentsByTenant(sb: App.Locals['srv'],tenantId:string,opts:{page?:number;pageSize?:number;search?:string;sortKey?:string|null;sortDir?:'asc'|'desc'}={}){const sortColumn=opts.sortKey&&STUDENT_SORT_COLUMNS.has(opts.sortKey)?opts.sortKey:'created_at';const ascending=opts.sortKey?opts.sortDir!=='desc':false;return paginatedQuery<Record<string,unknown>>(sb,'students',tenantId,{select:'id, admission_no, student_no, first_name, middle_name, last_name, date_of_birth, gender, admission_date, nationality, grade, status, created_at',order:{column:sortColumn,ascending},page:opts.page,pageSize:opts.pageSize,filters:q=>q.is('deleted_at',null),search:opts.search?{term:opts.search,columns:STUDENT_SEARCH_COLUMNS}:undefined});}
export async function getRecentRemedialStudents(sb:App.Locals['srv'],tenantId:string,limit:number){return getRecentStudents(sb,tenantId,limit);}
export async function getRecentStudents(sb:App.Locals['srv'],tenantId:string,limit:number){const{data}=await sb.from('students').select('id,admission_no,first_name,last_name,grade,created_at').eq('tenant_id',tenantId).is('deleted_at',null).order('created_at',{ascending:false}).limit(limit);return data??[];}
export async function getStudentList(sb:App.Locals['srv'],tenantId:string){const{data}=await sb.from('students').select('id,admission_no,student_no,first_name,last_name').eq('tenant_id',tenantId).is('deleted_at',null).eq('status','active').order('first_name');return data??[];}
